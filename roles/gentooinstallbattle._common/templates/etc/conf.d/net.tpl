config_{{ item.interface }}="{{ item.config }}"
routes_{{ item.interface }}="{{ item.routes }}"
dns_servers_{{ item.interface }}="{{ item.dns_servers }}"
dns_domain_{{ item.interface }}="{{ item.dns_domain }}"
